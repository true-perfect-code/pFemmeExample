
Offene Punkte:
+ WebApi Projekt
- Lokale Speicherung im Browser (IndexedDB) => bei iOS/iMax Konzept über json-Datei
+ Unit-Test Projekt
+ MSIX Projekt (MS Store)
- Visual Studio Vorlage

- Login-Komponente so anpassen, dass die Css-Klassen für header, content und Footer übertragen werden können (z.B. wie bei Justin)
- Login => doppelte eingabefelder eliminieren
- App.manifest in die Vorlage Blazor Wpf einfügen (damit es beim Publish nicht probleme gibt)
- Splash Screen muss entsprechend über Style.xml eingebunden werden => doku anpassen